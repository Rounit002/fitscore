# Cordova build artifacts — all regenerated by the build.
platforms/
plugins/
www/
node_modules/

# Icons/splash are rasterised from Frontend/public/favicon.svg by `npm run gen:res`.
res/

# Release signing material must never be committed.
build.json
*.keystore
*.jks
